{
  // the connectionString json element was created when the web app
  //  was created within the security level of Individual Accounts
  //  The code to handle the security will be using a DB connection
  //    name of DBConnection THEREFORE you will NOT remove this connection string

 
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
